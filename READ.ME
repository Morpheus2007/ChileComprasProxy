ChileComprasProxy

- Para ejecutar el proxy a nivel local, se debe contar en el equipo con:
	* Visual Studio 2017 o superior
	* SDK Net Core 2.0 
	* SDK Net Core 2.1

- Ingresar a la carpeta ChileComprasProxy/Backend
- Ejecutar archivo ChileComprasProxy.sln
- Establecer proyecto ChileComprasProxy.Backend.WebApi como proyecto de inicio
- Lo que ejecutara el proxy en modo local se debe adherir a URL, el path "/swagger"
